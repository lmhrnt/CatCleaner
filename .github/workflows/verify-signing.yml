name: CatCleaner Signing Verification (disabled)

on:
  workflow_dispatch:

jobs:
  signing-disabled:
    runs-on: macos-latest
    steps:
      - name: Signing infrastructure not configured
        run: |
          echo "CatCleaner signing verification is intentionally disabled."
          echo "Do not reuse the upstream Mac Sai notary profile or Developer Team ID."
          exit 1
